# 业务配置在随机管理入口填写。默认宿主反代访问 127.0.0.1:18080，无需创建 .env。
# 只有需要改变容器部署参数时才复制本文件为 .env。
# CONTROL_BIND_IP=127.0.0.1
# CONTROL_PORT=18080
# NODELANE_VERSION=0.2.0
# NODELANE_ADMIN_PATH=/replace-with-your-private-entry
# NODELANE_GEOIP_URL=https://download.db-ip.com/free/dbip-city-lite-{month}.mmdb.gz
# 仅配合 compose.network.yaml：
# CONTROL_DOCKER_NETWORK=1panel-network
